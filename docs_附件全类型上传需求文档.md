# 需求文档：邮箱功能附件全类型上传改造

> 项目：table-match-wails（Go + Wails 电商工具）
> 版本基线：v1.0.3
> 编写：臭宝（Hermes Agent）
> 日期：2026-09-07

---

## 1. 背景与现状

### 1.1 现状描述
当前 v1.0.3 的邮箱发送模块（前端 `frontend/src/main.js` → `mailPickAttach()`，后端 `backend/email.go` → `SendEmail()`）存在以下限制与缺陷：

| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| P0 | 文件选择对话框默认过滤器为 `*.xlsx;*.xlsm;*.csv`，用户以为只能传表格 | `app.go SelectFile()`（全部调用方共用） | 邮件/核对/开票选择器全部受限 |
| P0 | 多附件时 MIME 结束边界 `--boundary--` 写在 for 循环内部 | `email.go:92` | ≥2 个附件邮件结构损坏，第二个起丢失 |
| P0 | 收件人 `Rcpt()` 只取逗号分隔的第一个 | `email.go:183` | 多收件人只有第一人收到 |
| P1 | `base64.StdEncoding.EncodeToString` 输出不按 RFC 2045 折行（76 字符） | `email.go:140-142` | 正文/附件超长行，部分 SMTP 服务器拒收 |
| P1 | 附件读取用 `os.ReadFile` 全量进内存 + `string(data)` 非必要拷贝 | `email.go:81-91` | 大附件内存翻倍 |
| P2 | 附件名含引号/换行等字符未转义 | `email.go:87-90` | 畸形文件名可破坏 MIME 头 |

### 1.2 用户原始需求（原话）
> "邮箱功能 的附件需要改成所有文件都能上传 做成一个需求文档按步骤维修"

### 1.3 目标
1. 附件选择对话框支持**任意文件类型**（图片/压缩包/PDF/视频/任意扩展名），且默认视图即"所有文件"。
2. 后端 MIME 构造对**多附件、任意类型、中文名**均正确。
3. 顺手修复审计发现的 5 个严重缺陷（多附件 MIME / 多收件人 / 图片输出嵌套 / 透明变黑底 / 自然人反逻辑），一次发版解决。

### 1.4 非目标（本期不做）
- SMTP 授权码 DPAPI 加密存储（已有独立方案，另行排期）。
- 分块/流式发送超大附件（>50MB 场景另立项）。
- HTML 富文本邮件正文。

---

## 2. 需求详情

### 2.1 需求 R1：文件选择对话框"所有文件类型"

**R1.1** 新增专用附件选择函数（不动其他模块的共用选择器，避免误伤）：

```
App.SelectAttachFile() -> (string, error)
```
- 对话框 Filters 第一项 = `所有文件 (*.*)`，再附常用类型分组（图片、压缩包、文档）方便快速过滤；
- 允许多选（`OpenFileDialog` → 若干平台限制时退化为逐个添加，前端已有 `mailPickAttach` 累加逻辑，天然兼容）。

**R1.2** 前端 `mailPickAttach()` 改调 `SelectAttachFile()`。

**R1.3** 后端不做扩展名白名单/黑名单（需求即"所有文件"），但做**安全校验**：
- 单文件 ≤ 25MB（QQ 邮箱普通附件上限 50MB，留余量；超限报错而非静默失败）；
- 文件必须存在且可读；
- 附件名去除路径分隔符，仅保留 `filepath.Base()`，过滤 `"` 与换行符（防 MIME 头注入）。

### 2.2 需求 R2：MIME 构造健壮化（多附件正确性）

- 结束边界移出循环（只写一次）；
- 每个附件 part：`Content-Type: application/octet-stream`（通用二进制，接收端按扩展名自行识别）+ RFC 2231/2047 编码文件名；
- 正文 base64 按 76 字符折行（RFC 2045）；
- 附件 base64 同样折行。

### 2.3 需求 R3：多收件人

- `to` 按逗号（半角/全角）、分号、空格分隔拆分，逐个 `Rcpt()`；
- 任一收件人被服务器拒绝 → 整体报错并列出被拒地址（发件是原子操作，避免"部分成功无人知晓"）。

### 2.4 需求 R4：顺带修复的缺陷（同版发布）

| Bug | 位置 | 修复方案 |
|-----|------|----------|
| 图片转换输出目录嵌套（out/out/out…） | `image.go RunImgConvert` | 遍历入口先比较 `abs(path) == absOut` 即 `SkipDir`，并给 `convertOne` 输出加 `.tmp` 原子写 |
| 透明 PNG/WebP 合成白底失效（黑底） | `image.go convertOne` | 放弃 `*image.RGBA` 类型断言，改 `image/draw.Draw` 白底通用合成（NRGBA/NYCbCrA 全覆盖） |
| 自然人标识默认"是"反逻辑 | `invoice.go` | 默认"否"；仅当类型列明确含"个人/自然人"才写"是"；选"否"但税号为空 → 校验报错提示补税号 |

---

## 3. 验收标准

1. ✅ 附件按钮可选 `.zip .7z .exe .mp4 .psd` 等任意类型，对话框默认"所有文件"。
2. ✅ 同时添加 ≥2 个附件（含中文名），QQ 邮箱网页端收信显示全部附件且可下载、内容校验和一致。
3. ✅ 收件人填 `a@x.com，b@x.com`（含全角逗号）两人都收到。
4. ✅ 透明 PNG 转 JPG 背景为白色（不再是黑色）。
5. ✅ 图片转换输出目录选在源目录内时，重复执行不产生嵌套目录。
6. ✅ 发票导入类型为"公司"但税号为空 → 生成时报错提示，不静默变自然人。
7. ✅ `go build ./...` 与 `go vet ./...` 通过。
8. ✅ 附件 >25MB 时明确报错"附件过大"。

---

## 4. 实施步骤（维修顺序）

| 步骤 | 动作 | 文件 | 预计 |
|------|------|------|------|
| S1 | 后端 `email.go` 重写 MIME 构造（R2）+ 折行 + 多收件人（R3）+ 大小校验（R1.3） | backend/email.go | 30min |
| S2 | `app.go` 新增 `SelectAttachFile()` | app.go | 10min |
| S3 | 前端 `main.js` `mailPickAttach` 切换到新 API + wailsjs 绑定 | frontend/src/main.js | 10min |
| S4 | 修 `image.go` 两个 bug | backend/image.go | 20min |
| S5 | 修 `invoice.go` 自然人逻辑 | backend/invoice.go | 10min |
| S6 | `go build ./... && go vet ./...` 编译验证（OECT Docker golang 镜像） | - | 15min |
| S7 | 打包发版 v1.0.4 + CHANGELOG（用户在 Windows 侧执行 wails build） | - | - |

---

## 5. 风险与回退

- MIME 重写影响所有邮件（含无附件纯文本路径）→ 回归用例必须含"无附件纯文本"场景；
- `draw` 合成会引入 `image/draw` 依赖（标准库，无三方风险）；
- 发版前保留 v1.0.3 release 与 tag，出问题可即时回退。

---

*本文档由审计发现 + 用户需求整理，修复 diff 见交付说明。*
